<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="HeaderTheme" parent="Theme.AppCompat.Light" />

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="Mavver" parent="AppTheme">
        <item name="colorPrimary">@color/appmain</item>
        <item name="colorPrimaryDark">@color/appmaindark</item>
        <item name="colorAccent">@color/appmain</item>
    </style>

    <style name="Mavver.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="Mavver.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

</resources>
